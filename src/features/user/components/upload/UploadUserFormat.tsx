import {
  FormActions,
  FormBody,
  FormLayout,
} from "@/shared/components/organism/layouts/form-layout/FormLayout";

export const UploadUserFormat = () => {
  return (
    <FormLayout>
      <FormBody>
        {/* checkboxs */}
        {/* checkboxs */}
      </FormBody>
      <FormActions>
        <Button startIcon="download" type="submit" variant="outlined">
          Descargar formato
        </Button>
      </FormActions>
    </FormLayout>
  );
};
